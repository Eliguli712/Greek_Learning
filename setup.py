from setuptools import setup, find_packages

setup(
    name="greek_learning",
    version="0.1.0",
    description="Ancient Greek semantic analysis framework",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[],
)
